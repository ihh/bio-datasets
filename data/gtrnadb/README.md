# GtRNAdb

Genomic tRNA database sequences, fetched via the Rfam RF00005 (tRNA family) full alignment. Contains ~700k tRNA sequences pre-aligned against the tRNA covariance model with secondary structure annotation.

## Fetch

See [fetch.py](fetch.py) for the download script.
